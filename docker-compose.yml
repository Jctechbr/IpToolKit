services:
  iptoolkit:
    build:
      context: /home/ironman/IpToolKit
      dockerfile: Dockerfile
    image: iptoolkit:latest
    restart: unless-stopped
    ports:
      - "127.0.0.1:5010:80"
    environment:
      - TZ=America/Sao_Paulo
